[phases.setup]
nixPkgs = ["python310", "ffmpeg", "libopus", "libsodium"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt",
    "pip install --upgrade yt-dlp",
    "pip install --force-reinstall PyNaCl"
]

[start]
cmd = "python main.py"
