{
  "providers": ["python"],
  "buildCommand": "pip install --upgrade pip && pip install -r requirements.txt && pip install --upgrade yt-dlp && pip install --force-reinstall PyNaCl",
  "startCommand": "python main.py"
}
